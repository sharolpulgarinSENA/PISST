services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: pisst_dev
      POSTGRES_USER: pisst
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - pisst_postgres_data:/var/lib/postgresql/data

volumes:
  pisst_postgres_data:
